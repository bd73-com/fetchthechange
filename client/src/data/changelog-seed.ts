/**
 * Manual changelog entries for releases predating GitHub Releases automation.
 * These are merged into the generated changelog.ts by script/sync-changelog.ts.
 * If a GitHub Release exists with the same version, the release takes precedence.
 */

export const changelogSeed: { version: string; date: string; body: string }[] = [];
